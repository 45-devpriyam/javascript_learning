"use strict"; //treat all js code as a newer version

//alert("Hello") // It shows error because we run nodejs not browser


console.log(3+3);

/*
number, string, boolean, undefined, symbol
*/
console.log(typeof 3+3);
//typeof to know the data type in javascript.